name: Print Hello World
on:
  push:
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello World"
      - name: Repo Clone
        uses: actions/checkout@v4
      - name: Add numbers
        run: node hello.js